# Зависимости для деплоя в Yandex Cloud Functions.
# Yandex ищет requirements.txt в КОРНЕ архива (рядом с index.py) и сам делает
# pip install при сборке версии. Держите этот файл в синхроне с app/requirements.txt.
fastapi>=0.110,<1.0
SQLAlchemy[asyncio]>=2.0,<2.1
asyncpg>=0.29
pydantic>=2.6,<3.0
pydantic-settings>=2.2
python-multipart>=0.0.9
httpx>=0.27
PyYAML>=6.0
boto3>=1.34
# Выгрузка аналитических отчётов (§11 ТЗ 03.08.2026): .xlsx и .pdf.
# Импортируются лениво (app/services/analytics_export.py) — на холодный старт и
# остальной API не влияют.
openpyxl>=3.1
reportlab>=4.0
